// Cron scheduler disabled - use Vercel Cron in production or manual endpoint calls in development
// See vercel.json for production cron configuration
// For development, manually call: /api/cron/sync-matches and /api/cron/calculate-scores
